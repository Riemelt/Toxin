mixin button(vars = {})
  -
    const {
      icon,
      href,
      theme = 'default',
      size = 'default',
      text = '',
      hovered = false,
      type = 'button',
    } = vars

    const className = 'button'
    let classNames = className + ` js-${className}`

    const tag = href ? 'a' : 'button'

    const buttonAttributes = {
      a: {
        href,
      },
      button: {
        type,
      },
    }

    const materialIcon = {
      arrowForward: 'arrow_forward',
    }

    const sizeClass = {
      default: '',
      small: ` ${className}_size_small`,
      medium: ` ${className}_size_medium`,
      big: ` ${className}_size_big`,
      large: ` ${className}_size_large`,
    }

    classNames += sizeClass[size]

    const themeClass = {
      default: '',
      bordered: ` ${className}_theme_bordered`,
      filled: ` ${className}_theme_filled`,
    }

    classNames += themeClass[theme]

  if hovered
    - classNames += ` ${className}_hovered`
    
  #{tag}(class=classNames)&attributes(buttonAttributes[tag])
      span(class=`${className}__body`)
        span(class=`${className}__text`)= text
        if icon
          span(class=`${className}__icon`)
            span(class='material-icons')= materialIcon[icon]