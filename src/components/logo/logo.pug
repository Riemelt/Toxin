mixin logo(vars = {})
  -
    const {
      src   = "",
      href  = "/mock-address/change-me",
      theme = "",
    } = vars

    const className = "logo"
    let classNames  = className

    const themeClass = {
      uiKit: ` ${className}_ui-kit`,
    }

    classNames += themeClass[theme]

  a(
    class=classNames,
    href=href
  )
    img(
      class=`${className}__icon`
      alt="toxin logo"
      src=require(`@images/${src}`)
    )